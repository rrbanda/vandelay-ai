# =============================================================================
# Vandelay Search - Secret Template
# =============================================================================
# THIS IS A TEMPLATE - Do NOT add real secrets here!
#
# For OpenShift: Create secrets via CLI (recommended):
#   oc create secret generic vandelay-search-secrets \
#     --from-literal=NEO4J_PASSWORD=your-password \
#     --from-literal=OPENAI_API_KEY=your-key \
#     -n vandelay-search
#
# For Podman Desktop: Use env file instead (see env.template)
#   cp env.template .env
#   # Edit .env with your values
#   podman run --env-file .env ...
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: vandelay-search-secrets
  namespace: vandelay-search
  labels:
    app.kubernetes.io/name: vandelay-search
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # PLACEHOLDER VALUES ONLY - Create real secrets via CLI!
  NEO4J_PASSWORD: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
